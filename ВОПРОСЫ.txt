1. Автотесты на 4-ый пункт "отрисовка границ поля"
непонятно зачем проверять. и так же, судя по всему, в проверке будет фигурировать та же реализация, что и в распределении самих "кубиков"


2. По 5-ому пункту, если я правильно понимаю:
characterGenerator - функция, которая выдергивает в необходимом количестве разрешенных персонажей из входящего массива (массива с персонажами для игрока или же для компьютера)

а функция generateTeam уже создает команду, на основе того, что выдернет генератор.

Верно ли понимаю? Просто я задаю массивы с возможными вариантами персонажей, но тогда Webpack ругается, я их закомментировал, и он отстал.

2. По поводу запрета на создание базового класса, не понимаю как написать правильно
Сперва прям в классе Character написал так:

    if (new.target) {
      throw new Error('Нельзя создавать экземпляр базового класса')
    }

но тогда, нельзя было создать другие классы, наследуемые от этого

тогда написал так:

    if (new.target.type === 'generic') {
      throw new Error('Нельзя создавать экземпляр базового класса')
    }

вроде как работает, наследуемые классы создаются, но тогда не врубаюсь, как написать тесты правильно
Тот, что я написал, он не проходит


3. Правильно ли я понимаю, что здесь опечятка?
attackAfter = Math.max(attackBefore, attackBefore * (1.8 - life) / 100)
и должно быть
attackAfter = Math.max(attackBefore * (1.8 - life / 100))
?


